.d-flex.justify-content-evenly
        p Page #{page} of #{totalPages}

        if page > 1
            a(href='/admin/user/?page=' + (page - 1)) Previous
        if page < totalPages
            a(href='/admin/user/?page=' + (page + 1)) Next
